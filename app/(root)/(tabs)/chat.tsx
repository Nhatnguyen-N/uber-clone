import { View, Text } from "react-native";
import React from "react";

type Props = {};

const Chat = (props: Props) => {
  return (
    <View>
      <Text>chat</Text>
    </View>
  );
};

export default Chat;
